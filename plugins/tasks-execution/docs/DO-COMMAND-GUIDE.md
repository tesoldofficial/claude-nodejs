# Claude Code `/do` Command - Complete Guide

Полный цикл разработки задачи: от бизнес-анализа до протестированной реализации.

## 📦 Что было создано

### Command `/do <task-name> [description]`
**Расположение**: `~/.claude/commands/do.md`

**Назначение**: Автоматизированный workflow разработки задачи через 8 фаз:
- Phase 0: Business Analysis (интерактивный анализ требований)
- Phase 1: System Design (архитектурное проектирование)
- Phase 2: Implementation (написание кода)
- Phase 3: Bug Hunting (поиск и исправление багов, до 5 итераций)
- Phase 4: Code Cleanliness (проверка качества кода)
- Phase 5: Test Creation (автогенерация тестов)
- Phase 6: Test Execution (запуск тестов)
- Phase 7: Test Fixing (исправление упавших тестов, до 3 итераций)
- Phase 8: Final Summary (итоговый отчёт)

## 🚀 Использование

### Синтаксис

```bash
/do <task-name> [description or "начни заново"]
```

**Параметры**:
- `<task-name>` - **обязательный**, короткий идентификатор задачи
- `[description]` - опциональное, описание задачи или команда перезапуска

### Примеры

**Создать новую задачу:**
```bash
/do AUTH-123 "Реализовать JWT авторизацию с refresh токенами"
```

**Продолжить существующую:**
```bash
/do AUTH-123
```

**Перезапустить заново:**
```bash
/do AUTH-123 начни заново
```

**Создать без описания** (будет задан вопрос):
```bash
/do user-login
```

## ✅ Валидация названия задачи

### Валидные названия (короткие идентификаторы)

✅ `AUTH-123` - ID из трекера (Jira, YouTrack, etc.)
✅ `user-login` - kebab-case название
✅ `V-37` - версия + номер
✅ `fix-auth-bug` - тип + краткое описание
✅ `feature-payment` - префикс + название

### Невалидные названия (это описания, не идентификаторы)

❌ `"Реализовать логин пользователя"` - полное предложение
❌ `"Add JWT authentication to the API"` - полное предложение
❌ `"Fix the authentication bug in login flow"` - предложение с глаголом

### Что проверяется

- Длина < 50 символов
- Не начинается с глагола действия + пробел (Реализовать, Add, Fix, etc.)
- Нет знаков препинания (точка, запятая, кроме дефиса)
- Не более 4 слов

### Ошибка при невалидном названии

```
❌ ERROR: Передано описание задачи вместо названия

Вы передали: "Реализовать JWT авторизацию"

Это похоже на описание задачи, а не на название.

Пожалуйста, используйте формат:
  /do <короткое-название> "Описание задачи"

Например:
  /do AUTH-123 "Реализовать JWT авторизацию с refresh токенами"
```

## 📁 Структура задачи

После выполнения `/do task-name` создаётся:

```
.claude-project/tasks/task-name/
├── TASK.md                      # Полная бизнес-спецификация (Phase 0)
├── SYSTEM-DESIGN.md             # Архитектурный дизайн (Phase 1)
├── BUGS-ITERATION-1.md          # Отчёты bug hunter (Phase 3)
├── BUGS-ITERATION-N.md          # Финальный отчёт (без P0 багов)
├── CLEANLINESS.md               # Отчёт code cleanliness (Phase 4)
├── TEST-FIX-PLAN-1.md           # Планы исправлений тестов (Phase 7)
├── SUMMARY.md                   # Checkpoint + прогресс (обновляется постоянно)
├── CHANGES.diff                 # Git diff изменений
├── files/                       # Пустая папка для примеров/доков (пользователь заполняет)
└── tests/                       # Автосгенерированные интеграционные тесты (Phase 5)
    ├── package.json
    ├── helpers.js               # Утилиты (login, apiCall, assert, etc.)
    ├── run-all.js               # Запуск всех тестов последовательно
    ├── .env.test.example        # Шаблон credentials
    ├── .env.test                # Реальные credentials (gitignored)
    ├── README.md                # Документация тестов
    ├── 01-happy-path.js         # Нумерованные тестовые сценарии
    ├── 02-edge-case.js
    └── ...
```

## 🔄 Режимы работы

### 1. Новая задача (папка не существует)

```bash
/do NEW-TASK "Описание"
```

**Flow**:
1. Создаёт `.claude-project/tasks/NEW-TASK/`
2. Запускает Phase 0 (Business Analysis)
3. Проходит все 8 фаз последовательно
4. Завершает с полностью протестированной реализацией

### 2. Продолжить существующую (Checkpoint Resume)

```bash
/do EXISTING-TASK
```

**Flow**:
1. Проверяет `.claude-project/tasks/EXISTING-TASK/` - существует ✅
2. Читает `SUMMARY.md` → определяет последнюю завершённую фазу
3. Продолжает с **следующей** незавершённой фазы
4. Завершает оставшиеся фазы

**Пример**: Если в прошлый раз остановились на Phase 2 (Implementation), продолжит с Phase 3 (Bug Hunting).

### 3. Перезапустить заново

```bash
/do EXISTING-TASK начни заново
# или
/do EXISTING-TASK start over
# или
/do EXISTING-TASK reset
```

**Flow**:
1. Обнаруживает ключевое слово ("начни заново")
2. Делает backup: `tasks/EXISTING-TASK/` → `tasks/EXISTING-TASK-backup-20251101-120000/`
3. Создаёт свежую папку `tasks/EXISTING-TASK/`
4. Запускает с Phase 0 (начало)

## 📊 Детальное описание фаз

### Phase 0: Business Analysis (Plan Mode)

**Цель**: Полностью понять задачу перед началом разработки

**Процесс**:
1. **Проверка project docs**: Если `.claude-project/project/` не существует, предлагает запустить `/init-project`
2. **Создание/чтение TASK.md**:
   - Если есть description в команде → записывает в TASK.md
   - Если нет → спрашивает у пользователя
3. **Итеративное рассуждение** (из CLAUDE.md):
   - Задаёт себе вопросы: что неясно? какие edge cases? какие зависимости?
   - Формулирует гипотезы на основе project context
   - Критический анализ: что может пойти не так?
   - Итерирует 1-20 раз до полной ясности
   - Если застрял после 3-5 итераций → `AskUserQuestion`
4. **Финализация TASK.md**:
   - Бизнес-контекст (цель, пользователи, ценность)
   - Функциональные требования
   - Нефункциональные требования (производительность, безопасность)
   - Пользовательские сценарии (Given-When-Then)
   - Критерии приёмки
   - Технические ограничения
   - Зависимости
   - Out of scope
5. **Обновление SUMMARY.md**: Первый checkpoint

**Выход**: Полная бизнес-спецификация в `TASK.md`

### Phase 1: System Design

**Цель**: Создать архитектурный дизайн на уровне интерфейсов

**Процесс**:
1. Запускает **agent system-designer** с входными данными:
   - Business Requirements: `TASK.md`
   - Project Context: `.claude-project/project/`
2. Agent создаёт техническую спецификацию:
   - Какие сервисы/классы создать/модифицировать
   - Сигнатуры методов с параметрами и типами
   - Потоки данных между компонентами
   - Стратегия обработки ошибок
   - Обработка edge cases
3. Сохраняет результат в `SYSTEM-DESIGN.md`
4. Обновляет `SUMMARY.md`

**Выход**: Детальный архитектурный дизайн в `SYSTEM-DESIGN.md`

### Phase 2: Implementation

**Цель**: Написать production-ready код

**Процесс**:
1. Запускает **agent code-implementer** с входными данными:
   - Business Requirements: `TASK.md`
   - Technical Design: `SYSTEM-DESIGN.md`
   - Project Context: `.claude-project/project/`
2. Agent пишет код, следуя **10 правилам качества**:
   1. No unused parameters
   2. Methods under 50 lines
   3. Imports at top
   4. Clear, descriptive naming
   5. Enums/Constants over magic strings
   6. NEVER use 'any' type
   7. Loops/Maps over repetitive if-else
   8. ConfigService over hardcoded env vars
   9. Decorators for cross-cutting concerns
   10. Additional quality checks
3. Сохраняет `git diff` в `CHANGES.diff`
4. Обновляет `SUMMARY.md` со списком изменённых файлов

**Выход**: Реализованный код, готовый к проверке

### Phase 3: Bug Hunting (Итеративный)

**Цель**: Найти и исправить все P0 (критические) баги

**Процесс**:
1. **Loop** (максимум 5 итераций):
   - Запускает **agent bug-hunter-analyzer**
   - Анализирует код на:
     - Data flow issues (null checks, type coercion, shadowing)
     - Business logic violations
     - Error handling gaps
     - Security issues (API keys in logs, SQL injection, XSS)
     - Performance issues (N+1 queries)
   - Сохраняет отчёт в `BUGS-ITERATION-{N}.md`
   - Категоризует: **P0** (Critical), **P1** (High), **P2** (Medium)
   - Если P0 == 0 → **exit loop** ✅
   - Если P0 > 0 → запускает **code-implementer** для исправления P0 → повторяет
2. **Safety limit**: После 5 итераций останавливается (даже если P0 остались)
3. Обновляет `SUMMARY.md` с историей итераций

**Выход**: Код без критических багов (или предупреждение, если лимит достигнут)

### Phase 4: Code Cleanliness Review

**Цель**: Проверить соблюдение стандартов качества

**Процесс**:
1. Запускает **agent code-cleanliness-reviewer**
2. Проверяет на 10 типов проблем:
   1. Unused parameters
   2. Large methods (>50 lines)
   3. Dynamic imports in code
   4. Unclear naming
   5. Strings instead of enums
   6. Usage of 'any'
   7. Excessive if-else
   8. Hardcoded environment variables
   9. Decorator opportunities
   10. Additional issues (duplicated code, console.log, unused imports)
3. Сохраняет отчёт в `CLEANLINESS.md`
4. Если есть **Critical** issues → запускает **code-implementer** для исправления
5. Обновляет `SUMMARY.md`

**Выход**: Код соответствует стандартам качества

### Phase 5: Test Creation

**Цель**: Создать comprehensive интеграционные тесты

**Процесс**:
1. **Анализ** для извлечения тестовых сценариев:
   - `TASK.md` → user stories, acceptance criteria
   - `SYSTEM-DESIGN.md` → API endpoints, data flow, error cases
   - Написанный код (git diff) → актуальная реализация
2. **Извлекает сценарии**:
   - **Happy Path**: основной успешный сценарий
   - **Edge Cases**: граничные условия
   - **Error Cases**: обработка ошибок
   - **Integration Flows**: полные цепочки (регистрация → логин → доступ)
3. **Создаёт структуру** `tests/`:
   - `package.json` (axios, dotenv, form-data)
   - `helpers.js` (login, apiCall, uploadFile, assert, etc.)
   - `run-all.js` (sequential runner)
   - `.env.test.example` (template credentials)
   - `README.md` (документация тестов)
4. **Генерирует тестовые файлы**:
   - Нумерованные: `01-happy-path.js`, `02-edge-case.js`, etc.
   - Формат: Given-When-Then steps с console.log
   - Exit code 1 при ошибке
5. Обновляет `SUMMARY.md`

**Выход**: Полный набор тестов в `tasks/{name}/tests/`

### Phase 6: Test Execution

**Цель**: Запустить тесты и проверить реализацию

**Процесс**:
1. **Запускает приложение** в background:
   ```bash
   npm start  # run_in_background: true
   sleep 5  # ждём готовности
   ```
2. **Устанавливает зависимости** тестов:
   ```bash
   cd tasks/{name}/tests && npm install
   ```
3. **Запускает тесты**:
   ```bash
   npm test
   ```
4. **Анализирует результаты**:
   - Exit code 0 → все прошли ✅
   - Exit code 1 → есть упавшие ❌
5. **Cleanup**: Убивает background процесс (`KillShell`)
6. Обновляет `SUMMARY.md`

**Выход**:
- Если все прошли → переход к Phase 8 (Final Summary)
- Если упали → переход к Phase 7 (Test Fixing)

### Phase 7: Test Failure Analysis & Fix Planning

**Цель**: Исправить упавшие тесты

**Процесс**:
1. **Loop** (максимум 3 итерации):
   - Анализирует вывод упавших тестов
   - Создаёт план исправлений (`TEST-FIX-PLAN-{N}.md`):
     - Какие тесты упали
     - Какие ошибки (assertion failed, exception, timeout)
     - Предполагаемые причины
     - Предлагаемые исправления
   - Показывает план через `TodoWrite`
   - Запускает **code-implementer** для исправления
   - Перезапускает тесты (повторяет Phase 6 steps)
   - Если все прошли → **exit loop** ✅
   - Если ещё упали → повторяет
2. **Safety limit**: После 3 итераций останавливается
3. Обновляет `SUMMARY.md`

**Выход**: Все тесты проходят (или предупреждение, если лимит достигнут)

### Phase 8: Final Summary

**Цель**: Создать итоговый отчёт о задаче

**Процесс**:
1. Компилирует полную информацию из всех фаз
2. Обновляет `SUMMARY.md` с финальным отчётом:
   - Overview (название, дата, длительность, статус)
   - Phase Summary (каждая фаза с результатами)
   - Files Created/Modified
   - Acceptance Criteria Status (✓ каждый критерий)
   - Next Steps
3. Выводит финальное сообщение в консоль

**Выход**: Полностью завершённая и протестированная задача

## 🔧 SUMMARY.md как Checkpoint

`SUMMARY.md` обновляется **после каждой фазы** и содержит:

```markdown
# Task task-name - Progress Summary

## Current Phase
**Status**: In Progress / Completed
**Phase**: 3 - Bug Hunting
**Started**: 2025-11-01 12:00
**Last Updated**: 2025-11-01 12:30

## Phase History

### ✅ Phase 0: Business Analysis (COMPLETED)
- Duration: 15 minutes
- Iterations: 3
- Output: TASK.md

### ✅ Phase 1: System Design (COMPLETED)
- Duration: 20 minutes
- Agent: system-designer
- Output: SYSTEM-DESIGN.md

### 🔄 Phase 3: Bug Hunting (IN PROGRESS)
- Iteration: 1
- Bugs found: 2 P0, 5 P1, 3 P2
- Next: Fix P0 bugs

## Pending Phases
- [ ] Phase 4: Code Cleanliness
- [ ] Phase 5: Test Creation
- ...
```

**При повторном запуске** `/do task-name`, Claude:
1. Читает `SUMMARY.md`
2. Определяет последнюю завершённую фазу
3. Продолжает со следующей незавершённой

## 🛡️ Safety Mechanisms

### 1. Bug-Fix Loop Limit

**Проблема**: Может зациклиться на исправлении багов
**Решение**: Максимум 5 итераций Phase 3
**После лимита**: Предупреждение + ручная проверка

### 2. Test-Fix Loop Limit

**Проблема**: Тесты могут не проходить бесконечно
**Решение**: Максимум 3 итерации Phase 7
**После лимита**: Предупреждение + ручная отладка

### 3. Валидация названия задачи

**Проблема**: Пользователь может передать описание вместо названия
**Решение**: Детектирование описания (длина, глаголы, пунктуация)
**При невалидном**: Останавливает выполнение, показывает ошибку

### 4. Проверка project documentation

**Проблема**: Без `.claude-project/project/` контекст неполный
**Решение**: Проверка в Phase 0, предложение запустить `/init-project`
**Варианты**: Запустить /init-project / Продолжить без контекста / Отменить

## 🧪 Формат тестов

### Структура тестового файла

```javascript
/**
 * Test: User Login with JWT
 *
 * Expected behavior:
 * - User can login with valid email/password
 * - Receives JWT access token
 * - Token is valid for 1 hour
 */

require('dotenv').config({ path: require('path').join(__dirname, '.env.test') });
const { login, apiCall, assert, assertEqual } = require('./helpers');

async function testUserLogin() {
  console.log('\n' + '='.repeat(60));
  console.log('1️⃣  TEST: User Login with JWT');
  console.log('='.repeat(60));

  try {
    // Step 1: User logs in with valid credentials
    console.log('\n📝 Step 1: Login with valid credentials');
    const email = process.env.TEST_USER_EMAIL;
    const password = process.env.TEST_USER_PASSWORD;
    const token = await login(email, password);
    assert(token, 'Token should be received');
    console.log('✅ Step 1 completed');

    // Step 2: Verify token is valid
    console.log('\n📝 Step 2: Access protected resource with token');
    const response = await apiCall(token, 'GET', '/api/profile');
    assertEqual(response.status, 200, 'Should access protected resource');
    console.log('✅ Step 2 completed');

    console.log('\n' + '='.repeat(60));
    console.log('✅ SUCCESS: User Login test passed');
    console.log('='.repeat(60));

  } catch (error) {
    console.error('\n' + '='.repeat(60));
    console.error('❌ ERROR:', error.message);
    if (error.response) {
      console.error('Status:', error.response.status);
      console.error('Data:', JSON.stringify(error.response.data, null, 2));
    }
    console.error('='.repeat(60));
    process.exit(1);
  }
}

testUserLogin();
```

### helpers.js utilities

```javascript
// Login and get token
async function login(email, password) {
  const response = await axios.post(`${API_URL}/auth/login`, { email, password });
  return response.data.token;
}

// Make API call with token
async function apiCall(token, method, endpoint, data = null) {
  const config = {
    method,
    url: `${API_URL}${endpoint}`,
    headers: token ? { Authorization: `Bearer ${token}` } : {},
    data
  };
  return await axios(config);
}

// Upload file
async function uploadFile(token, endpoint, filePath, fieldName = 'file') {
  const form = new FormData();
  form.append(fieldName, fs.createReadStream(filePath));

  return await axios.post(`${API_URL}${endpoint}`, form, {
    headers: {
      ...form.getHeaders(),
      Authorization: `Bearer ${token}`
    }
  });
}

// Assertions
function assert(condition, message) {
  if (!condition) throw new Error(`Assertion failed: ${message}`);
}

function assertEqual(actual, expected, message) {
  if (actual !== expected) {
    throw new Error(`${message}\nExpected: ${expected}\nActual: ${actual}`);
  }
}
```

## 📈 Примеры использования

### Пример 1: Новая задача с описанием

```bash
/do AUTH-123 "Реализовать JWT авторизацию. Пользователи должны иметь возможность зарегистрироваться, войти, выйти и обновить токен. Токены живут 1 час, refresh токены - 7 дней."
```

**Результат**:
1. ✅ Phase 0: Уточняет детали (алгоритм хеширования? база данных?)
2. ✅ Phase 1: Проектирует AuthService, AuthController, UserEntity, DTOs
3. ✅ Phase 2: Пишет код (NestJS + TypeORM + JWT)
4. ✅ Phase 3: Находит 3 P0 бага (null check, token expiry logic), исправляет за 2 итерации
5. ✅ Phase 4: Исправляет 2 критичных cleanliness issues (unused param, magic string)
6. ✅ Phase 5: Создаёт 8 тестов (регистрация, логин, обновление токена, выход, etc.)
7. ✅ Phase 6: Все тесты проходят
8. ✅ Phase 8: Готово и протестировано

**Время**: ~2-3 часа

### Пример 2: Продолжить прерванную задачу

```bash
# Вчера запускали, но прервали на Phase 2
/do AUTH-123
```

**Результат**:
1. Читает `SUMMARY.md` → последняя фаза: Phase 2 (Implementation) completed
2. Продолжает с Phase 3 (Bug Hunting)
3. Завершает оставшиеся фазы 3-8

### Пример 3: Перезапустить с нуля

```bash
/do AUTH-123 начни заново
```

**Результат**:
1. Бэкапит `tasks/AUTH-123/` → `tasks/AUTH-123-backup-20251101-153000/`
2. Создаёт свежую `tasks/AUTH-123/`
3. Запускает все фазы 0-8 с начала

### Пример 4: Невалидное название

```bash
/do "Реализовать JWT авторизацию с refresh токенами"
```

**Результат**:
```
❌ ERROR: Передано описание задачи вместо названия

Вы передали: "Реализовать JWT авторизацию с refresh токенами"

Это похоже на описание задачи, а не на название.

Валидные названия задач (короткие идентификаторы):
  ✅ AUTH-123
  ✅ user-login

Пожалуйста, используйте формат:
  /do <короткое-название> "Описание задачи"

Например:
  /do AUTH-123 "Реализовать JWT авторизацию с refresh токенами"
```

**Выполнение остановлено.**

## 🎯 Best Practices

### 1. Используйте осмысленные названия задач

✅ **Хорошо**:
- `AUTH-123` (из трекера)
- `user-profile-edit` (описательное)
- `fix-memory-leak` (тип + проблема)

❌ **Плохо**:
- `task1` (неинформативное)
- `asdf` (непонятное)
- `Реализовать профиль` (это описание)

### 2. Давайте детальные описания

При первом запуске чем подробнее описание, тем меньше вопросов в Phase 0:

```bash
/do USER-PROFILE "Реализовать редактирование профиля. Пользователь должен иметь возможность изменить имя, email, пароль и аватар. Email должен быть уникальным. При смене email требуется верификация. Пароль хешируется с bcrypt."
```

### 3. Используйте папку files/

Если у вас есть:
- Примеры запросов/ответов
- Скриншоты UI
- Дополнительная документация
- Образцы данных

Положите в `tasks/{name}/files/` **до** запуска `/do` или во время Phase 0.

### 4. Проверяйте SUMMARY.md между сессиями

Если прервали задачу:
1. Откройте `tasks/{name}/SUMMARY.md`
2. Посмотрите на какой фазе остановились
3. Прочитайте "Next Phase" для контекста
4. Запустите `/do {name}` для продолжения

### 5. Запускайте /init-project перед /do

Если проект ещё не инициализирован:
```bash
/init-project
# Создаст .claude-project/project/ с контекстом

/do TASK-1 "Описание задачи"
# Будет использовать контекст из .claude-project/project/
```

### 6. Проверяйте тесты локально

После выполнения `/do`:
```bash
cd .claude-project/tasks/{name}/tests/
cp .env.test.example .env.test
# Отредактируйте .env.test с реальными credentials
npm test
```

### 7. Используйте "начни заново" аккуратно

Команда перезапуска делает backup, но лучше:
- Сначала просмотрите текущее состояние
- Решите, действительно ли нужен full restart
- Возможно, достаточно просто продолжить

## ⚠️ Ограничения и известные проблемы

### 1. Автогенерация тестов может быть неполной

**Проблема**: Claude создаёт тесты на основе спецификации, но может упустить edge cases

**Решение**:
- Просмотрите созданные тесты в `tasks/{name}/tests/`
- Добавьте дополнительные сценарии вручную
- Следуйте формату существующих тестов

### 2. Bug hunting может не найти все баги

**Проблема**: Статический анализ имеет ограничения

**Решение**:
- После завершения проведите manual code review
- Запустите линтеры (ESLint, etc.)
- Проверьте логи при выполнении тестов

### 3. Test-fix loop может зациклиться

**Проблема**: Некоторые тесты могут требовать manual debugging

**Решение**:
- Если достигнут лимит 3 итераций, просмотрите:
  - `TEST-FIX-PLAN-3.md` - последний план
  - Вывод тестов
  - Код в контексте ошибок
- Исправьте вручную и перезапустите тесты

### 4. Требуется .claude-project/project/

**Проблема**: Без project context качество дизайна ниже

**Решение**:
- Всегда запускайте `/init-project` перед первым `/do`
- Поддерживайте project docs актуальными

## 🔄 Интеграция с другими командами

### /init-project → /do workflow

```bash
# Шаг 1: Инициализировать проект
/init-project
# Создаёт .claude-project/project/ с бизнес-контекстом и архитектурой

# Шаг 2: Реализовать первую задачу
/do TASK-1 "Описание первой задачи"
# Использует контекст из .claude-project/project/

# Шаг 3: Синхронизировать project docs (если изменилась архитектура)
/init-project
# SYNC MODE обновит документацию под изменения

# Шаг 4: Следующая задача
/do TASK-2 "Описание второй задачи"
```

### Git workflow

```bash
# Создать feature branch
git checkout -b feature/AUTH-123

# Запустить задачу
/do AUTH-123 "Описание"

# Проверить изменения
git status
git diff

# Commit
git add .
git commit -m "feat: Implement JWT auth (AUTH-123)

- Added AuthService, AuthController
- Implemented login, register, refresh token flows
- Added integration tests

Task: .claude-project/tasks/AUTH-123/SUMMARY.md"

# Push и создать PR
git push -u origin feature/AUTH-123
```

## 📊 Статистика и метрики

После выполнения задачи `SUMMARY.md` содержит:

- **Duration**: Сколько времени заняла вся задача
- **Phase breakdown**: Время на каждую фазу
- **Bug iterations**: Сколько раз исправляли баги
- **Test iterations**: Сколько раз исправляли тесты
- **Files changed**: Количество изменённых файлов
- **Tests created**: Количество тестовых сценариев

**Пример**:
```markdown
## Overview
**Total Duration**: 2h 35m

## Phase Durations
- Phase 0: 15m
- Phase 1: 20m
- Phase 2: 45m
- Phase 3: 25m (2 iterations)
- Phase 4: 10m
- Phase 5: 20m
- Phase 6: 5m
- Phase 7: 15m (1 iteration)

## Metrics
- Files Changed: 12
- Tests Created: 8
- Bug Iterations: 2
- Test Fix Iterations: 1
```

## 🎉 Готово к использованию!

Command `/do` полностью реализован и готов к использованию.

**Следующие шаги**:
1. Убедитесь, что проект инициализирован (`/init-project`)
2. Запустите первую задачу (`/do TASK-1 "Description"`)
3. Следуйте инструкциям Claude на каждом этапе
4. Проверяйте результаты в `.claude-project/tasks/TASK-1/`

**Документация**:
- Command definition: `~/.claude/commands/do.md`
- This guide: `~/DO-COMMAND-GUIDE.md`

---

*Created: 2025-11-01*
*Generated by Claude Code development workflow*
